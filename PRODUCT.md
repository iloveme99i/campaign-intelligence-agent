# Product — Campaign Intelligence

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 连锁零售与本地生活商家的用户运营、增长运营和产品经理，需要在活动结束后判断目标是否达成、变化发生在哪条用户路径、成本是否值得，以及下一轮该改什么。
- 项目同时作为 AI Agent 产品经理求职作品接受评估，但面试官不是界面的主要用户，产品不能为“展示技术”牺牲真实任务体验。

## Product Purpose

导入活动配置、用户行为事件、转化结果与成本数据后，围绕一个活动完成目标校验、范围澄清、确定性核算、路径诊断和证据留存。成功不是生成一段看似合理的总结，而是让使用者能核对结论、修正口径并重新计算。

## Product Workflow

1. 系统读取活动目标、周期、指标与投入，商家只补充需要支持的经营决定、优先风险和数据中不可见的业务变化。
2. 商家确认活动期、对比期、筛选范围和退款口径，形成不可混用的 Scope ID。
3. 系统确定性计算目标、经营结果、用户路径、方案差异与成本，生成证据清单。
4. DeepSeek 在同一证据范围内区分事实、解释假设和待验证项，形成下一轮行动合同。
5. 使用者确认实验对象、分组、主指标、MDE 与实验流量后，系统从同一目标群体的已留存证据确定性计算下一轮实验样本量、预计周期和上线前检查项。
6. 业务负责人选择按建议执行、修改后执行或暂不采用，记录原因与最终动作；决定连同落档时质量门、证据、口径和限制条件一起导出，口径变更产生新版本。
7. 到复查节点后回填实际执行状态、对照与处理样本、测量设计、护栏和数据依据；系统检查固定样本、样本比例失衡、统计门槛与 MDE，并记录扩大、迭代、停止或继续收数的后续决定。

## Positioning

Agent 负责提出并验证复盘路径；关键业务指标由只读、受限的数据工具确定性计算，每条结论保留查询、数据版本与复盘口径，避免把语言模型的推断伪装成事实。

## Operating Context

使用者从一次已结束或正在运行的活动进入，确认目标、活动期、对比期、目标人群和归因窗口；系统先核算目标结果，再定位曝光、参与、关键行为、转化和成本路径中的变化，并区分事实、解释假设与待验证项。

## Capabilities and Constraints

- 数据输入为四类 CSV：活动配置、匿名行为事件、交易结果、活动成本；当前为本地单用户项目。
- 金额以整数分进入系统；订单贡献额按净收入减订单直接成本计算，活动投入另列，不包含租金、人力等全部经营费用，不等于净利润。
- 只允许读取导入后不可变的数据快照；模型不能修改数据或绕过查询预算。
- Agent 模型使用 DeepSeek API。真实效果、延迟和调用成本必须在 API Key 配置后实测，不能用脚本模型结果冒充。
- 当前不公开发布，不处理真实顾客姓名、电话等个人信息。

## Evidence on Hand

- 上游代码基线：`datahub-project/analytics-agent`，Apache-2.0，提交 `466e8ab3db967450db9578c7f649925f96e3de8c`。
- 本地合成数据用于功能验证，并明确标注为合成数据；尚无真实商家数据、真实用户访谈或线上 A/B 结果。
- 需求边界、指标口径、Agent 决策链与评测门槛位于 `docs/product/`；内容明确区分已实现事实与待验证假设。

## Product Principles

1. 先给可核对的经营事实，再给模型解释。
2. 只询问数据无法可靠推断的信息，不让用户替系统填写已知内容。
3. 口径变更产生新的计算结果，旧结果保留且不混用。
4. 界面服务判断与追查，Agent 技术过程默认退居次级。
5. 不捏造指标、用户、收益、实验或效果。

## Accessibility & Inclusion

键盘可操作、清晰焦点、正文不低于 16px；颜色不能作为涨跌、成功或错误的唯一表达。
